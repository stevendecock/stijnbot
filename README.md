# stijnbot
Stijn Slackbot

**Installation**

Command line:

`npm install --save botkit`

Set token on environment:
on windows:
`set token=<MY TOKEN>`
on mac:
`export token=<MY TOKEN>

Run:

`node slack_bot.js`
